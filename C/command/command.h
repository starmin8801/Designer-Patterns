#ifndef COMMAND_H
#define COMMAND_H

#include <stdlib.h>
#include <stdarg.h>

typedef struct {
    size_t size;
    void* (*ctor)(void *_self, va_list *params);
    void* (*dtor)(void *_self);
    void (*execute)(const void *_self);
} Command;

#endif
